#include "main.h"

/**
 * _abd - Start point
 * @n: compute absolute value of integer
 * description: compute the integer
 *
 * Return: 0
 */

int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
