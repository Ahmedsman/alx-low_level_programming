#include <stdio.h>
/**
 *main- The beginning of the program
 *
 *Return: without zero if no erros otherwise return non-zero value.
 */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}
