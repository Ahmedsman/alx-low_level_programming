#include <stdio.h>
/**
 *
 * main - entry piont
 *
 * description: using main function
 * this programe print alphabet in lower case
 * return: 0
 */
int main (void)
{
char ch
for (ch = 'a' ; ch <= 'z' : ch++) 
{
	putchar (ch);
}
{
	if (ch != 'e' && ch != 'q')
{
	putchar(ch);
}
}
pitchar('\n');
return(0);
}
