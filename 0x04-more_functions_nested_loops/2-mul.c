#include "main.h"
/**
 * mul - function that multiply 2 integers
 * @a: first integer
 * @b: second integer
 * Return: pucduct value of the integers
 */
int mul(int a, int b)
{
	int mul(int a, int b)
	{
		return (a * b);
	}
}
