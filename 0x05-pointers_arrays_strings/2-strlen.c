#include <stdio.h>
/**
 * _strlen - function that returns the length of a string
 * @s: the string
 * Return: length of string
 */

int _strlen(char *s)
{
	int length = 0;
	while (s[length])
		lenght++;
	return (lenght)
}
